name: ddirc
description: "A minimal, modern IRC client, on a Rust core."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  ddirc_bridge:
    path: rust_builder
  flutter_rust_bridge: 2.13.0
  freezed_annotation: ^3.1.0

  # First-party, and the only thing settings need: a small key/value store on
  # every platform we target. Pinned like every other dependency.
  shared_preferences: 2.5.3

  # Hides the native title bar so the app can draw its own window controls.
  # Pinned like every other dependency.
  window_manager: 0.5.2

  # A tray icon, so a window closed to the background has somewhere to be
  # closed *to*. Hiding a window with no way back is a lost app, not a
  # feature, which is why this is not optional once the window can hide.
  # Same family as window_manager, and inert on mobile. Pinned like every
  # other dependency.
  tray_manager: 0.5.3

  # Desktop notifications, so a message can reach someone whose window is
  # behind something else. Same family as window_manager and tray_manager
  # above, and it covers Windows, macOS and Linux in one go.
  #
  # Android is deliberately not here: MainActivity already owns a MethodChannel
  # and a notification permission flow for the foreground service, so a second
  # notification is a channel id and a method, not a dependency. This package
  # has no Android implementation and is inert there. Pinned like every other
  # dependency.
  local_notifier: 0.1.6

  # Profile passwords: Android Keystore, and DPAPI on Windows. Never
  # shared_preferences. Pinned like every other dependency.
  flutter_secure_storage: 11.0.0

  # Picking a file to send, and choosing where a received one is saved.
  # First-party, and it hands back a path rather than bytes — which is what the
  # core wants: a file is streamed from disk to the socket and never crosses the
  # FFI boundary, so sending a large one costs no more memory than a small one.
  # Pinned like every other dependency.
  file_selector: 1.1.0

  # Where log files may be written. Every platform answers this differently and
  # two of them (Android, iOS) can only be asked at runtime, so there is no
  # honest way to hardcode it. First-party. Pinned like every other dependency.
  path_provider: 2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  build_runner: ^2.15.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The tray icon. A tray is a native control and takes a file rather than a
  # widget, so this is the one place the mark has to ship as an asset. Written
  # by `make icons` from the same MarkSpec as every other icon, so it cannot
  # drift from the one on the taskbar beside it.
  assets:
    - assets/tray/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
